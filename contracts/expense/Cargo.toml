[package]
name = "passbook-expense"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "20.5.0"
ethnum = "1.5.1"

[dev-dependencies]
soroban-sdk = { version = "20.5.0", features = ["testutils"] }
